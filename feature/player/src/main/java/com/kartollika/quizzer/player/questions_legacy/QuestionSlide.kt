package com.kartollika.quizzer.player.questions_legacy

/*
@OptIn(ExperimentalPagerApi::class)
@Composable
fun QuestionSlide(modifier: Modifier, slides: Slides) {
  HorizontalPager(modifier = modifier, count = slides.pages.size) { page ->
    val slide = slides.pages[page]
    Column {
      slide.pictures?.forEach { picture ->
        Image(painter = painterResource(id = drawable.editbox_dropdown_light_frame), contentDescription = null)
      }
      slide.text?.let { text ->
        Text(text = text)
      }
    }
  }
}*/
